{
  "name": "real-estate-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo run start:dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
